# Vercel environment audit

This document was produced by inspecting actual source usage — every row below traces to a real `process.env[...]` read (or an explicit absence of one) found by searching the repository, not to assumption or to what `.env.example` merely lists. No real secret value appears anywhere in this file.

**Updated for Gate 11.** The prior version of this document (product-repair audit) found `DATABASE_URL` completely unconsumed and flagged SQLite persistence as incompatible with Vercel's serverless filesystem. Gate 11 closes that gap: `DATABASE_URL` is now genuinely consumed when `MARKED_STORAGE_DRIVER=postgres` is set. See `evidence/production-persistence/` for the full implementation, schema, and proof status.

**Updated for the KeeperHub execution-wiring task.** `KEEPERHUB_API_KEY` and `KEEPERHUB_BASE_URL` move out of "NOT REQUIRED" in this update — the live web app now genuinely calls KeeperHub (`simulateContractCall` unconditionally once a job is ARMED; `executeContractCall` only when `MARKED_ENABLE_KEEPERHUB_EXECUTION=true`, which is a new, separate, default-off safety flag — see below). `ENABLE_MAINNET_WRITE` is also now genuinely read from `process.env` by the deployed app (previously true only of an offline script). See `apps/web/src/lib/execution/`, `evidence/system-audit/findings.md` (KEEPERHUB-WIRING-001), and `docs/ARCHITECTURE.md`.

## Read this first: Postgres mode is implemented and proven against a real hosted database

Gate 11 added `PostgresFulfillmentJobStore` (`packages/db/src/postgres-fulfillment-job-store.ts`) and an explicit driver-selection factory (`apps/web/src/lib/job-store.ts`). With `MARKED_STORAGE_DRIVER=postgres` and a real `DATABASE_URL`, the app's persistence is durable Postgres, not a local file — the Vercel-incompatibility the prior audit flagged is solved, both architecturally and now by real exercise. A real hosted Neon Postgres database was migrated (`pnpm db:migrate`) and the full mandatory test suite — the shared contract battery, concurrent-worker races, forced-failure transaction rollback, restart/reconnect, and JSONB serialization round-trip — was run against it for real: all passing. See `evidence/production-persistence/hosted-production-proof.md` for the full account and `evidence/production-persistence/gate11-result.md` for the verdict.

Setting `MARKED_STORAGE_DRIVER=postgres` on a real deployment (Vercel or otherwise) still requires supplying that deployment's own real `DATABASE_URL` and running `pnpm db:migrate` against it first — this document proves the code path works, it does not configure any specific deployment on your behalf. Leaving `MARKED_STORAGE_DRIVER` unset (the default) keeps the app on SQLite, which is honest but does not survive Vercel's serverless filesystem for ARM/APPROVE/DISARM (same caveat as before Gate 11). Read-only pages — the landing page, `/demo`, `/proof/gate6`, `/evidence`'s static content, `/docs`, and `/app/new`'s resolution/preview up to but not including arming — are unaffected either way.

## Variables actually consumed by the deployed app

| Variable | Required? | Secret? | Server or public? | Consumed by | Purpose | Production value shape | What breaks if absent |
|---|---|---|---|---|---|---|---|
| `MARKED_STORAGE_DRIVER` | No — defaults to `sqlite` | No | Server-only | `apps/web/src/lib/job-store.ts` (`buildStore`) | Explicitly selects the persistence backend: `sqlite` (default) or `postgres`. No `NODE_ENV`-based inference. | `sqlite` or `postgres` | Unset or `sqlite`: local-file SQLite (see the Vercel-incompatibility caveat above). `postgres` with no `DATABASE_URL`: throws `PersistenceConfigurationError` — fails closed, never silently falls back to SQLite. Any other value: same fail-closed error. |
| `DATABASE_URL` | Conditionally — required if and only if `MARKED_STORAGE_DRIVER=postgres` | Yes | Server-only. Never `NEXT_PUBLIC_*`, never serialized to HTML, never logged (verified — see `evidence/production-persistence/security-scan.md`) | `apps/web/src/lib/job-store.ts` (`buildStore`), `packages/db/src/postgres-fulfillment-job-store.ts`, `packages/db/src/migrate.ts` | The Postgres connection string for production persistence | `postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require` (a real hosted or self-hosted Postgres) | If `MARKED_STORAGE_DRIVER=postgres` and this is absent: `PersistenceConfigurationError`, fails closed, ARM/APPROVE/DISARM and the recovery sandbox return a typed configuration error rather than a fake success. If `MARKED_STORAGE_DRIVER` is unset/`sqlite`, this variable is ignored entirely. |
| `MARKED_DEMO_SESSION_TOKEN` | Conditionally — required for the demo-workspace login and every ARM/APPROVE/DISARM call to ever succeed | Yes | Server-only (never sent to the client in any form — httpOnly cookie carries only a `"1"` marker, not the token itself) | `apps/web/src/lib/auth-server.ts`, `apps/web/src/lib/session.ts`, `apps/web/src/lib/fulfillment-actions.ts`, `apps/web/src/app/app/actions.ts` | Gates the documented hackathon-demo authentication boundary (Gate 7) for mutating operations | Any non-empty string (e.g. a random 32+ char token) | `enterDemoWorkspace` redirects back to `/app` without authenticating; the demo login form shows "server has no demo session token configured"; every ARM/APPROVE/DISARM call returns 401. Read-only pages are unaffected. |
| `MARKED_ENABLE_KEEPERHUB_EXECUTION` | No — defaults to disabled | No | Server-only | `apps/web/src/lib/execution/keeperhub-config.ts` (`isLiveExecutionEnabled`) | The dedicated production safety gate for the real KeeperHub write path (`executeContractCall`), independent of `ENABLE_MAINNET_WRITE`. Deliberately left unset/`false` on this deployment: `MARKED_DEMO_SESSION_TOKEN`'s auth model lets any visitor who opens a demo session call APPROVE, which is not a safe boundary to put a real blockchain write behind yet (see finding F-03). | `true` to enable (not recommended until auth is hardened) | Unset/anything other than `"true"`: `approveAndExecuteJob` throws `LiveExecutionDisabledError` before any state mutation or network call — the job stays at `AWAITING_APPROVAL`. The UI shows this plainly rather than a dead button. |
| `KEEPERHUB_API_KEY` | Conditionally — required for the "check eligibility & simulate" step on any ARMED job, and for real execution | Yes | Server-only, never `NEXT_PUBLIC_*`, never sent to the browser in any form | `apps/web/src/lib/execution/keeperhub-config.ts`, reached from `apps/web/src/lib/execution/prepare.ts` and `apps/web/src/lib/execution/approve-and-execute.ts` | Authenticates every KeeperHub API call (`simulateContractCall`, and — only when `MARKED_ENABLE_KEEPERHUB_EXECUTION=true` — `executeContractCall`) | Opaque KeeperHub API key | Absent: `prepareJobForApproval` cleanly refuses (`REFUSAL_WORKFLOW_POLICY_VIOLATION`, reason names `KEEPERHUB_API_KEY`) rather than throwing an unhandled error — a job simply cannot be prepared for approval. |
| `KEEPERHUB_BASE_URL` | No | No (a plain URL, not a secret) | Server-only | `apps/web/src/lib/execution/keeperhub-config.ts` | Overrides KeeperHub's default API base URL | Full HTTPS URL | Falls back to `packages/keeperhub`'s hardcoded default (`https://app.keeperhub.com`) |
| `ENABLE_MAINNET_WRITE` | No — defaults to `false` | No | Server-only | `apps/web/src/lib/execution/keeperhub-config.ts` | Hard per-chain safety gate inside `packages/keeperhub` itself (`assertLocalPreconditionsForExecution`) — mainnet (chain 1) execution is refused locally, with zero network calls, unless this is exactly `"true"` | `true` or `false`/unset | Unset: any execution attempt against mainnet is refused before any KeeperHub call is made. This deployment's only currently-reachable chain (via the one source-verified governor deployment, chain 11155111/Sepolia) is unaffected either way. |
| `CACTUS_API_KEY` | No | Yes | Server-only | `packages/cactus/src/graphql-client.ts` (reached from `apps/web/src/lib/governance.ts`) | Enables the official authenticated Cactus GraphQL API as the first-choice resolution path | Opaque API key issued by Cactus/Tally | Nothing breaks — resolution automatically falls through to the live public-proposal-page path, which is the current default and the one this submission's evidence was captured against |
| `ETHEREUM_RPC_URL` | No | Effectively yes if it embeds a provider key (e.g. an Alchemy/Infura URL) | Server-only | `apps/web/src/lib/governance.ts` (`rpcUrlFor`) | Overrides the public default Ethereum mainnet RPC used to resolve Governor authorization during `/app/new` | Full HTTPS RPC URL | Falls back to viem's public default mainnet RPC (`https://ethereum.reth.rs/rpc`) — functions, but shared/rate-limited |
| `SEPOLIA_RPC_URL` | No | Effectively yes, same as above | Server-only | `apps/web/src/lib/governance.ts` (`rpcUrlFor`) | Same, for Sepolia-resolved proposals | Full HTTPS RPC URL | Falls back to viem's public default Sepolia RPC |
| `OPTIMISM_RPC_URL` | No | Effectively yes, same as above | Server-only | `apps/web/src/lib/governance.ts` (`rpcUrlFor`) | Same, for Optimism-resolved proposals (added Gate 12 CACTUS-LIVE-001, when chain 10 was added to the shared Governor-chain registry) | Full HTTPS RPC URL | Falls back to viem's public default Optimism RPC |
| `MARKED_AGENT_PROVIDER` | No | No | Server-only | `apps/web/src/lib/agent/provider.ts` | Explicitly selects "openrouter" or "anthropic" with no silent fallback between them | `openrouter` or `anthropic` | If unset, provider selection falls through to whichever of `OPENROUTER_API_KEY`/`ANTHROPIC_API_KEY` is present; if set but its matching key is absent, the agent reports "Agent unavailable" rather than substituting the other provider |
| `OPENROUTER_API_KEY` | No — the entire agent feature is optional | Yes | Server-only | `apps/web/src/lib/agent/provider.ts` | Authenticates `OpenRouterAgentProvider` | `sk-or-v1-...` | Every agent panel renders "Agent unavailable." Nothing else in the product is affected — the agent has zero execution authority regardless. |
| `OPENROUTER_MODEL` | No | No | Server-only | `apps/web/src/lib/agent/provider.ts` | Overrides the fixed free model id | e.g. `nex-agi/nex-n2.5-pro:free` | Falls back to the hardcoded, live-verified default free model |
| `ANTHROPIC_API_KEY` | No | Yes | Server-only | `apps/web/src/lib/agent/provider.ts` | Authenticates `AnthropicAgentProvider` | `sk-ant-...` | Agent unavailable if this is the selected/only provider and it's absent |
| `LLM_API_KEY` | No | Yes | Server-only | `apps/web/src/lib/agent/provider.ts` (fallback alongside `ANTHROPIC_API_KEY`) | Legacy/alternate name accepted for the same Anthropic path | `sk-ant-...` | Same as `ANTHROPIC_API_KEY` — redundant with it, not an independent variable |
| `MARKED_AGENT_MODEL` | No | No | Server-only | `apps/web/src/lib/agent/provider.ts` | Overrides the Anthropic provider's default model id | e.g. `claude-...` | Falls back to the provider's hardcoded default |

## NOT REQUIRED — declared but not consumed by the deployed app

Do not configure these on Vercel. Each was verified absent from every runtime code path the deployed app actually executes (source search, not assumption):

| Variable | Where it's declared | Why it does nothing today |
|---|---|---|
| `ENABLE_PUBLIC_RECEIPTS` | `.env.example`, `packages/config/src/env.ts` | Declared, defaulted, and unit-tested, but never read anywhere to gate access. `/proof/[id]` is unconditionally public regardless of this value's presence or absence. |
| `NEXT_PUBLIC_APP_ENV` | `.env.example`, `packages/config/src/env.ts` | Declared and defaulted, but never read or rendered anywhere in the app — despite a code comment in `env.ts` describing it as "safe to show in the UI as a badge," no page currently does so. |

`KEEPERHUB_API_KEY`, `KEEPERHUB_BASE_URL`, and `ENABLE_MAINNET_WRITE` were previously listed here as script-only/inert. They no longer are — see the consumed-variables table above. `scripts/gate5-keeperhub-execute.ts`/`scripts/prove-keeperhub-seam.ts` still read `KEEPERHUB_API_KEY` their own way (a regex against a local `.env.local` file) and remain unaffected by this change.

## What this means for `.env.example` and `packages/config`

`DATABASE_URL` has been promoted out of "NOT REQUIRED" as of Gate 11 — it is now genuinely load-bearing, conditionally on `MARKED_STORAGE_DRIVER=postgres`. `KEEPERHUB_API_KEY`/`KEEPERHUB_BASE_URL`/`ENABLE_MAINNET_WRITE` are promoted out of it now too, for the reasons above. `ENABLE_PUBLIC_RECEIPTS`/`NEXT_PUBLIC_APP_ENV` remain inert. `@marked/config`'s `EnvSchema`/`parseEnv` (the Zod schema in `packages/config`) still is not the mechanism that reads any of these — every consuming module (`job-store.ts`, `governance.ts`, `agent/provider.ts`, `session.ts`, and now `execution/keeperhub-config.ts`) reads `process.env` directly. Wiring `@marked/config` into the app's actual startup path remains a real, separate follow-up, not something this task performs.

## How to actually run Postgres on Vercel once a database exists

1. Set `DATABASE_URL` (server-only environment variable, in the Vercel project's environment variable settings — never in a committed file).
2. Run `pnpm db:migrate` with that same `DATABASE_URL` set locally (or via a one-off Vercel deployment step) — this creates `fulfillment_jobs`, `job_events`, `execution_claims`, and `schema_migrations`. It is safe to re-run.
3. Set `MARKED_STORAGE_DRIVER=postgres`.
4. Redeploy. `getAppStore()` will now construct `PostgresFulfillmentJobStore` instead of the local SQLite store.

Do not set `MARKED_STORAGE_DRIVER=postgres` before step 2 — the app will fail closed with `PersistenceConfigurationError` on first use if the tables don't exist yet (surfaced as a real Postgres "relation does not exist" error wrapped by the store), which is the correct, honest behavior — not a bug to work around.
