# Fixture B — Uniswap #20

Generated by `pnpm prove:cactus` (`scripts/prove-cactus-seam.ts`). Re-running that command regenerates every file in this directory from live sources — nothing here is hand-edited.

## Result: PASS (MATCH)

| Field | Value |
|---|---|
| Input URL | `https://www.tally.xyz/gov/uniswap/proposal/20` |
| Resolution method | `ssr_fallback` (documented SSR fallback — the official GraphQL API requires `CACTUS_API_KEY`, which was not configured in this environment; see `evidence/cactus/discovery.md`) |
| Source classification | `CURRENT_CACTUS` (live, currently-operated Cactus infrastructure — domain still `tally.xyz` pending the `cactushq.xyz` migration, see DEC-010) |
| Organization | Uniswap (`uniswap`) |
| Title | `Deploy Uniswap v3 on Gnosis Chain` |
| Chain | 1 (Ethereum mainnet) |
| Governor | `0x408ED6354d4973f66138C91495F2f2FCbd8724C3` (Cactus tags it `governorbravo`) |
| Onchain proposal ID | 20 |
| Cactus-reported status | `executed` |

## Independent onchain cross-check

RPC used: `https://ethereum-rpc.publicnode.com` (public fallback — no `ETHEREUM_RPC_URL` was configured; this is a reputable, widely-used public Ethereum RPC, read-only, no credentials).

- `eth_getCode(0x408ED6354d4973f66138C91495F2f2FCbd8724C3)` → non-empty bytecode (contract exists).
- `proposals(20)` on that address → succeeded, returned `id == 20`, `executed == true`.
- Cross-check: onchain `id` matches Cactus's `onchainProposalId` → **MATCH**. Onchain `executed` flag agrees with Cactus's reported `status: "executed"`.

This proves the Cactus-resolved coordinates point to a real, queryable Governor Bravo proposal — it does **not** prove anything about the proposal's authorized action bundle (targets/values/calldata). That reconstruction is explicitly Gate 2 work.

## Files in this directory

- `input.json` — the exact input given to the adapter.
- `raw-response.sanitized.json` — the `proposal`/`organization`/`governors` subset of the live page's embedded `__NEXT_DATA__` payload. No auth headers, cookies, or API keys were involved in this request (SSR fallback is unauthenticated), so nothing was redacted for secrecy; the `dehydratedState` and Sentry trace fields were excluded only because they are large and not relevant to identity resolution.
- `resolved.json` — this adapter's typed `ResolvedCactusProposal` output.
- `onchain-check.json` — the independent onchain sanity check result.
