# Evidence — Compound Proposal #220 (Governor Bravo)

Generated by `scripts/prove-governor-seam.ts` (`pnpm prove:governor`). Read-only: no execution, no KeeperHub call, no write of any kind at any point in producing this evidence.

## Coordinate

| Field | Value |
|---|---|
| Chain | Ethereum mainnet (chainId 1) |
| Governor | `0xc0Da02939E1441F497fd74F78cE7Decb17B66529` |
| Proposal ID | 220 |
| Governor family | `GOVERNOR_BRAVO` (detected via `initialProposalId()` probe, not guessed — see `packages/governor/src/family-detection.ts`) |

## Files in this directory

| File | Produced by | Contents |
|---|---|---|
| `coordinate.json` | script input | The `GovernorProposalCoordinate` passed to `resolveGovernorAuthorization` |
| `authorization.json` | `readBravoActions` + canonicalization | The immutable `GovernorBravoActionAuthorization` — version, chainId, governor, family, proposalId, and the full action bundle (target/value/signature/calldata per action), read live from `getActions(220)` |
| `lifecycle.json` | `readBravoLifecycle` | The mutable `GovernorLifecycleSnapshot` (state, eta, block) as of the resolution's pinned block — kept structurally separate from `authorization.json` per the lifecycle/authorization split (see `../canonical-encoding.md`) |
| `proof.json` | full script run | Everything above plus: `actionAuthorizationHash`, reproducibility comparison (two in-process resolutions), freeze/recheck result, caller-authority finding, and the lifecycle call plan |

## Headline result

Action authorization hash (version 1, Bravo domain):

```
0x29fab99c1fd3e796981fb2ae34b92280c7405bfafe6ef60882628ddec1dab28d
```

Reproduced byte-identical across three independent resolutions (two in-process, one in a fully separate process invocation) — see `../reproducibility.md` for the full comparison table.

## What the two authorized actions are

Read live from `getActions(220)` on the Governor Bravo contract — not from Cactus, not from proposal prose, not from a guessed ABI:

1. `setTargetReserves(address,uint104)` on `0x316f9708bB98af7dA9c68C1C3b5e79039cD336E3`
2. `deployAndUpgradeTo(address,address)` on `0x1EC63B5883C3481134FD50D5DAebc83Ecd2E8779`

This independently confirms — via live cryptographic read of the Governor contract, not by trusting any indexer — the action shape the PRD had described for this proposal.

## Lifecycle state at read time

`state(220)` returned `7` (`Executed`), matching both Cactus's own reported status (Gate 1A evidence) and Gate 1C's independent existence check. See `../bravo-methodology.md` for the `ProposalState` enum's authoritative ordering.

## Caller authority

`packages/governor/src/caller-authority.ts`'s finding for Bravo `execute`/`queue`: `PERMISSIONLESS` — no `msg.sender` check exists in the reference source. This is a read-only finding; no lifecycle call was made against this or any other proposal in Gate 2.
