# Gate 12 §42 — docs ↔ code ↔ evidence claim matrix

Focused on the claims most likely to have drifted given this gate's own findings, not a re-derivation of every claim in `CLAIMS.md` (which already carries its own PROVEN/TARGET/BLOCKED/REJECTED discipline, re-confirmed intact by this gate's regression).

| Claim | Where stated | Status after this gate | Note |
|---|---|---|---|
| "An authenticated mutating boundary with a real UX" (README.md, prior gate reports) | README.md, prior gate summaries | **PROVEN, but see F-03 for the precise, narrower meaning of "authenticated" here** | The existing phrasing ("a demo session cookie set by a name-only 'Enter demo workspace' form; the real token lives only server-side") is itself accurate and does not overclaim — it already discloses the name-only mechanism. This audit's contribution is making explicit (in `auth-audit.md`) that this mechanism provides essentially no secret-knowledge gate for the cookie path, which the existing copy does not contradict but also does not spell out as starkly. No copy change made — the existing language was found honest, not overclaiming, on closer reading. |
| `packages/core/src/auth.ts`'s own doc comment: "explicit authenticated demo session with allowlisted actor" | Source code comment | **STALE — no actor allowlist exists anywhere in the repository** | Internal doc-comment drift, not public-facing copy; noted here rather than silently edited, since correcting it either requires removing the aspirational claim or building the allowlist — a product decision, not a docs-typo fix. |
| Rate limiting on public agent Q&A | Previously an acknowledged gap in project history (per this gate's own brief) | **Was TRUE (gap existed) at the start of this gate; now FIXED (F-04)** | No public-facing copy claimed rate limiting existed, so there was no overclaim to correct — this was a known, disclosed limitation that is now closed. |
| "Zero blockchain writes in the deployed app" / "no hidden execution path" (implicit in prior gates' claims, e.g. Gate 11's "zero `sendTransaction`/signer/private-key usage") | DECISIONS.md, GATES.md | **PROVEN, now with an even stronger basis** | This gate's write-path-map (`repository-map.md`) is the most exhaustive confirmation of this claim yet produced — extends, does not contradict, prior gates' narrower per-gate confirmations. |
| `DATABASE_URL`/Postgres persistence claims (Gate 11) | VERCEL_ENVIRONMENT.md, CLAIMS.md | **Unaffected by this gate**, re-confirmed via full regression (Postgres tests still pass with the explicit opt-in, F-02's guard does not change the underlying persistence behavior) | — |
| Historical baseline numbers (Gate 8) | CLAIMS.md, GATES.md, README.md | **Unchanged, re-verified this gate** (`pnpm verify:historical`, exact match) | — |
| "Postgres mode is implemented and proven against a real hosted database" (VERCEL_ENVIRONMENT.md, post-Gate-11) | VERCEL_ENVIRONMENT.md | **Unaffected**, still accurate | — |

**No public-facing claim was found to require correction this gate.** The one internal-doc-comment drift (`auth.ts`'s "allowlisted actor" language) is noted for a future cleanup pass rather than edited here, since Gate 12's own scope is audit-and-narrow-fix, not copy editing.
