{
  "name": "@marked/config",
  "version": "0.0.0",
  "private": true,
  "description": "Strict, fail-safe environment parsing. A missing or malformed variable must never silently enable a mainnet write.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
