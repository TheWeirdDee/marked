{
  "name": "@marked/core",
  "version": "0.0.0",
  "private": true,
  "description": "Canonical Marked types, state machine, fulfillment commitment, hash-domain models, policy, reference model, receipt primitives, and invariant helpers. Marked fulfills governance authority; it does not create it.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "viem": "^2.21.55",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
