{
  "name": "@marked/db",
  "version": "0.0.0",
  "private": true,
  "description": "Persistence boundary. Drizzle ORM over PostgreSQL in deploy, ephemeral DB in tests. job_events is append-only — corrections append, they never rewrite history.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@marked/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
