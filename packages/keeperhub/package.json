{
  "name": "@marked/keeperhub",
  "version": "0.0.0",
  "private": true,
  "description": "KeeperHub execution client. Executes the frozen lifecycle call. Never contains a local fallback transaction broadcaster.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@marked/core": "workspace:*",
    "@marked/governor": "workspace:*",
    "viem": "^2.21.55"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
