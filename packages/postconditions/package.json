{
  "name": "@marked/postconditions",
  "version": "0.0.0",
  "private": true,
  "description": "EconomicPostconditionAdapter bundle — reads and asserts target protocol/token state. Reads and asserts. Never changes execution payload.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@marked/core": "workspace:*",
    "viem": "^2.21.55"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
